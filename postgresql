DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': 'test',
        'USER': 'subhash',
        'PASSWORD': 'Subbu@1592',
        'HOST': '127.0.0.1',
        'PORT': '5432',
    }
}



#in terminal
psql postgres            # to getinto postgresql
\l                      #to see the list of database
\c test                   # to get into test database

CREATE DATABASE test;           #to create database




#to create new user with password

CREATE USER sammy WITH PASSWORD 'pa$$word';  #interminal after psql postgres    

pip install psycopg2-binary         #install pip command
